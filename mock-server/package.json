{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "API simulada com json-server",
  "scripts": {
    "start": "json-server --watch ../mock-data/bd.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}

